document.addEventListener("DOMContentLoaded", function(event) {
    'use strict';

    // Focus iframe to allow keyboard input
    var iframe = document.getElementById('game-container__iframe');
    iframe.focus();
});
